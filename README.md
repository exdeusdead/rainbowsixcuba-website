# Rainbow Six CUBA Website v10

- New unique v10 image set by module.
- Global language persistence across all pages via `?lang=` and `localStorage`.
- Privacy, statistics, companion, terms and placeholders render in the selected language.
- Extension-specific privacy URL: `/companion/privacy_policy.html`.
